import type { TableConfig } from "$lib/types/_types.js";

export const TableDefaultConfig: TableConfig = {
	baseClass: 'table',
	striped: false,
	hover: false,
	bordered: false,
	dark: false,
	small: false,
	responsive: true
};